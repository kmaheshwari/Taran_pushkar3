<%= form_tag race_timing_ind_evnts_path do |f| %>

  <% @race_timing_ind_evnt.each do |r| %>
  <%@mname.each do |m|%>

  <%= m %><br><br>
  <%= hidden_field_tag :member_id, :value => i%>
                                <%= hidden_field_tag :event_id, :value => params[:race_timing_ind_evnt][:event_id]%>
                                <%= hidden_field_tag :age_group, :value => params[:age]%>
    <%= fields_for "race_timing_ind_evnt[#{r.member_id}]", r do |b| %>
      <%= render 'race_fields', f: b %>
    <% end %>
  <% end %>
  <% end %>

  

  <%= submit_tag %>
<% end %>