<h3 align="center"><strong>Race Timing for Group Events</strong></h3>


<%= form_for(@race_timing_grp_evnt) do |f| %>
  <% if @race_timing_grp_evnt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@race_timing_grp_evnt.errors.count, "error") %> prohibited this race_timing_grp_evnt from being saved:</h2>

      <ul>
      <% @race_timing_grp_evnt.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

        
      

<br><br>
<div class="col-xs-offset-7"><h4>Number of Students: <%= @mname.length%></h4></div>
<br>


      <div class="table-responsive row" >
          <div class="col-xs-8 col-xs-offset-2">
                  <table class="table table-bordered table-hover table-striped ">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Minutes</th>
                                <th>Present</th>
                            </tr>
                        </thead>
                        
                        <tbody>
                          <% @pagin.zip(@midlst).each do |mn,i| %>
                            <tr>
                                <td><%= mn %></td>
                                <%= hidden_field_tag "member_id[]", i%>
                                <%= f.hidden_field :group_event_id, :value => params[:race_timing_grp_evnt][:group_event_id]%>
                                <%= f.hidden_field :competetion_level_id, :value => params[:race_timing_grp_evnt][:gage]%>
                                <td><%= text_field_tag "mem[]" %></td>
                                <td><%= check_box_tag("", "", true, class: "checkbox") %></td>
                            </tr>
                            <% end %>
                          <%= paginate @pagin %>
                        </tbody>
                    </table>
                  <div class="control-group">
            <!-- Button -->
            <div class="controls span7 text-center">
              <button class="btn btn-success">Submit</button>
            </div>
                </div>
                
        </div>
    </div>
<%end%>
