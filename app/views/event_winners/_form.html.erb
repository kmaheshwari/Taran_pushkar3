<%= form_for(@event_winner) do |f| %>
  <% if @event_winner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_winner.errors.count, "error") %> prohibited this event_winner from being saved:</h2>

      <ul>
      <% @event_winner.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :points %><br />
    <%= f.number_field :points %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
