<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="modal-body ">
    <div class="well ">
      
      <fieldset>
        <div id="legend">
          <legend class=""><strong>Events Form</strong></legend>
        </div>

        <div class="form-group">
          <div class="col-sm-2 label-control">
            <%= f.label :"Event Name" %>
          </div>
          <div class="col-sm-4">
            <%= f.text_field :event_name, :placeholder => "Enter Event Name", :class => 'form-control' %>
          </div>
        </div>
    </div>
  </div>
 
  <br><br>

  <div class="form-group">     
          <div class="control-group">
            <!-- Button -->
            <div class="controls span7 text-center">
              <button class="btn btn-success">Submit</button>
              <%= link_to 'Back', events_path ,:class=>"btn btn-success"%>
            </div>
          </div>
        </div>
<% end %>
